#include <iostream>
#include <cmath>

using namespace std;

// functions
bool error();
void intro(int &introAns, int &paraAns, int &seriAns);
    // parallel
void paraSele(int &paraAns);
void calc_paraRT();
void calc_paraI();
    // series 
void seriSele(int &sereAns);
void calc_seriRT();
void calc_seriV();

int main() {

	int introAns;
	int paraAns;
	int seriAns;
	
	intro (introAns, paraAns, seriAns);
	
	/*int givenR;
	int givenI;
	int givenV;
	int R;
	int I;
	int V;*/

	return 0;
}

bool error() {
	// check if input is a non # or invalid
	if (cin.fail()) {
		cin.clear();
		cin.ignore(1000, '\n');
		cout<<"Invalid input. Please try again.\n";
		return true;
	}
	return false;
}

void intro(int &introAns, int &paraAns, int &seriAns) {
	// find circuit
	cout<<"What kind of circuit is being calculated for?"<<endl;
	cout<<"1) Parallel\n2) Series"<<endl;

	while(true) {
		cout<<"Selection: ";
		cin>>introAns;

		if(error()) continue;

		if(introAns<1||introAns>2) {
			cout<<"Invalid input. Please try again.\n";
			continue;
		}
		    break;
	}

	if(introAns==1) {
		paraSele(paraAns);
	} else if(introAns==2) {
		seriSele(seriAns);
	}
}

void paraSele(int &paraAns) {
    cout<<"\nWhat is being calculated?"<<endl;
    cout<<"1) Total resistance\n2) Resistor currents"<<endl;
    
    while(true) {
		cout<<"Selection: ";
		cin>>paraAns;

		if(error()) continue;

		if(paraAns<1||paraAns>2) {
			cout<<"Invalid input. Please try again.\n";
			continue;
		}
		    break;
	}

	if(paraAns==1) {
		calc_paraRT();
	} else if(paraAns==2) {
		calc_paraI();
	}
}

void calc_paraRT {
    
}

void calc_paraI {
    
}

void seriSele (int &sereAns) {
    cout<<"\nWhat is being calculated?"<<endl;
    cout<<"1) Total resistance\n2) Voltage drops"<<endl;
    
    while(true) {
		cout<<"Selection: ";
		cin>>sereAns;

		if(error()) continue;

		if(sereAns<1||sereAns>2) {
			cout<<"Invalid input. Please try again.\n";
			continue;
		}
		    break;
	}

	if(sereAns==1) {
		calc_sereRT();
	} else if(sereAns==2) {
		calc_sereV();
	}
}

void calc_sereRT {
    
}

void calc_sereV {
    
}
